{
  "name": "cfwokerblog",
  "version": "1.1.0",
  "description": "基于 Cloudflare Workers + D1 构建的轻量级博客系统（图片内嵌 base64，无需对象存储）",
  "main": "src/worker.js",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}